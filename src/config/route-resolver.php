<?php
return [ 
    /*
    |--------------------------------------------------------------------------
    | Use route prefix if default locale
    |--------------------------------------------------------------------------
    | If current locale same as default locale:
    | false - do not set it to [0] segment: if 'en' is default and 'en' is current then there is no prefix /about
    | true - then set it to [0] segment: if 'en' is default and 'en' is current then use prefix /en/about
    |
    */
];
